{% extends 'base.html.twig' %}

{% block title %} Hello RegisterController! {% endblock %}

{% block body %}
    <div class="table">
        <h1 class="text-center pb-5 pt-3"> Memberliste </h1>
        <table class="table">
            <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Vorname</th>
                <th scope="col">Email</th>
            </tr>
            </thead>
            <tbody>
            {% for user in user %}
                <tr>
                    <th scope="row">{{ user.id }} </th>
                    <td>{{ user.firstName }} </td>
                    <td>{{ user.email }} </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
